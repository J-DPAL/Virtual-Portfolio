spring:
  application:
    name: api-gateway

  security:
    jwt:
      secret: ${JWT_SECRET:dev-secret-only-use-locally}

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.portfolio: INFO

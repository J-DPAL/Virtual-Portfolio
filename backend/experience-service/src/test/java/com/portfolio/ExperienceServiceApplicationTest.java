package com.portfolio;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

@DisplayName("ExperienceServiceApplication Tests")
@Disabled("Requires full application context with database connectivity")
class ExperienceServiceApplicationTest {

  @Test
  @DisplayName("Disabled to avoid database dependency during unit tests")
  void disabledTest() {
    // Intentionally disabled
  }
}
